version: '3.8'

services:
  app:
    build: .
    container_name: checklist_app_prod
    ports:
      - "8000:8000"
    env_file:
      - ./.env.local
    restart: unless-stopped

# Note: this compose file is for running the built image (no bind-mounts).
